-- Threads is stored as a string in social_accounts.platform (VARCHAR), no schema change needed.
-- This migration is a no-op placeholder to document the addition of THREADS platform support.
SELECT 1;
